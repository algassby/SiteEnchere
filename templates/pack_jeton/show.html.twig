{% extends 'base.html.twig' %}

{% block title %}PackJeton{% endblock %}

{% block body %}
    <h1>PackJeton</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pack_jeton.id }}</td>
            </tr>
            <tr>
                <th>NbJetons</th>
                <td>{{ pack_jeton.nbJetons }}</td>
            </tr>
            <tr>
                <th>Descrption</th>
                <td>{{ pack_jeton.descrption }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ pack_jeton.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pack_jeton_index') }}">back to list</a>

    <a href="{{ path('pack_jeton_edit', {'id': pack_jeton.id}) }}">edit</a>

    {{ include('pack_jeton/_delete_form.html.twig') }}
{% endblock %}
